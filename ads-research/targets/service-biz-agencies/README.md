# Target: agencies selling TO service businesses (discovery, not a single page)

Not a single-advertiser teardown. Candidates were found with Foreplay text search,
then each brand's full live set was pulled by `brand_id`, where `running_duration`
is real (the discovery index returns it null or as a constant — unusable).

One snapshot only: 2026-09-10. Live tenure is sound; there is no died-between data,
so nothing here reports how long a dead ad ran.
